# -*- mode: sh -*-
# Bash config for login shells and then bashrc.

if [[ -e "${HOME}/.profile" ]] ; then
  source "${HOME}/.profile"
fi

if [[ -z "${BASHRC}" || -n "${TMUX}" ]] ; then
  if [[ -e "${HOME}/.profile" ]] ; then
    source "${HOME}/.bashrc"
  fi
fi
