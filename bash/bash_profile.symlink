# -*- mode: sh -*-
# Bash config for login shells and then bashrc.

if [[ -e "${HOME}/.profile" ]] ; then
  source "${HOME}/.profile"
fi

if [[ -e "${HOME}/.bashrc" ]] ; then
  source "${HOME}/.bashrc"
fi
